[project]
name = "shakal"
version = "0.1.0"
description = "ffmpeg wrapper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "prompt-toolkit>=3.0.52",
    "questionary>=2.1.1",
]
[project.scripts]
shakal = "shakal.main:main"
